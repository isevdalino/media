export class Comment {
    constructor(id, username, comment, createdAt) {
        this.id = id;
        this.username = username;
        this.comment = comment;
        this.createdAt = createdAt;
    }
};